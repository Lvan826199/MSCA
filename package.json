{
  "name": "msca",
  "version": "0.1.0",
  "description": "MSCA - 多设备移动端投屏控制系统",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uv run python __main__.py",
    "build": "cd frontend && npm run build",
    "build:web": "cd frontend && npm run build && mkdir -p ../dist/web && cp -r dist/* ../dist/web/",
    "electron:dev": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .\"",
    "electron:build": "npm run build && npm run backend:build && electron-builder",
    "backend:build": "bash scripts/build-backend.sh",
    "backend:verify": "bash scripts/verify-backend.sh",
    "dist:clean": "rm -rf dist/",
    "lint": "cd frontend && npm run lint",
    "format": "cd frontend && npm run format"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.0",
    "cross-env": "^7.0.3",
    "concurrently": "^9.1.0",
    "wait-on": "^8.0.1"
  },
  "build": {
    "appId": "com.msca.app",
    "productName": "MSCA",
    "directories": {
      "output": "dist/electron"
    },
    "files": [
      "electron/**/*",
      "frontend/dist/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "resources/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "extraResources": [
      {
        "from": "resources/",
        "to": "resources/"
      },
      {
        "from": "bin/android/",
        "to": "bin/android/"
      },
      {
        "from": "bin/ios/",
        "to": "bin/ios/"
      }
    ],
    "electronDownload": {
      "mirror": "https://npmmirror.com/mirrors/electron/"
    }
  }
}
