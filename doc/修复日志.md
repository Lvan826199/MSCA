# 修复日志

> 记录全项目 bug 排查与修复的过程、根因与验证结果，按时间倒序排列。
> 操作级流水详见 [memory/ai_step.md](../memory/ai_step.md)。

---

## 2026-06-11 — 第二轮：遗留缺陷清零（4 项）

第一轮排查中评估为"工程量大/需实测"而遗留的 4 项缺陷，本轮全部修复。

### 1. 设备旋转后 screen_size 不更新，旋转后 Android 反控失效（高）

- **位置**：`backend/app/scrcpy/server_manager.py`、`backend/app/scrcpy/protocol.py`
- **根因**：屏幕宽高只在握手时从 codec header 读取一次。scrcpy v3 server 会校验触控消息携带的宽高与设备当前显示尺寸一致，不一致则丢弃事件——设备旋转后宽高互换，所有触控被静默丢弃。
- **修复**：
  - `protocol.py` 新增 H.264 SPS 宽高解析（`_BitReader` Exp-Golomb 位读取器 + 防竞争字节剥离 + 裁剪单位计算，支持 Baseline/High profile、场编码、frame cropping）；
  - `server_manager.read_video_frame` 识别 frame meta 中 pts 字段的 `PACKET_FLAG_CONFIG`（bit 63）标志——设备旋转/分辨率变化时 server 会重发配置包（SPS/PPS），仅对配置包解析 SPS 并刷新 `_screen_width/_screen_height`，普通帧零额外开销。
- **测试**：新增 `tests/test_h264_sps.py`（9 个用例，测试内置规范级 SPS 位流写入器交叉验证解析器）+ `tests/test_scrcpy_device_messages.py` 旋转场景 2 个用例。

### 2. 后端端口探测 TOCTOU 与 --host 不一致（中）

- **位置**：`backend/__main__.py`
- **根因**：①探测 socket 关闭后到 uvicorn 实际 bind 之间端口可能被抢占，启动失败但 `.backend-port` 已写入旧值；②探测固定绑 `127.0.0.1`，传 `--host 0.0.0.0` 时探测结论不代表实际可绑定。
- **修复**：探测地址改为与 `args.host` 一致；uvicorn 启动失败（`SystemExit` 退出码 3，即 `STARTUP_FAILURE`）时清理端口文件并自动尝试下一个端口，最多 10 个。
- **测试**：新增 `tests/test_backend_entry.py`（3 个用例）；实测占用 18000 后启动，后端自动改用 18001 且健康检查通过、退出后端口文件正确清理。

### 3. parse_nal_units 起始码回退计算错误（低，潜伏）

- **位置**：`backend/app/scrcpy/protocol.py`
- **根因**：对非开头位置的 3 字节起始码（`00 00 01`），切片回退按位置而非实际起始码长度判断，返回的 NAL 数据会多带前一个无关字节。当前调用方只消费 `nal_type`（单独计算，结果正确）故未爆发；本轮新增的 SPS 解析开始消费 NAL 数据，必须先修。
- **修复**：扫描统一按 `00 00 01` 匹配并记录前缀实际起始位置（前一字节为 `00` 即 4 字节起始码），单元边界按实际前缀长度回退。
- **测试**：`tests/test_h264_sps.py` 中混合 3/4 字节起始码边界用例 2 个。

### 4. Electron 打包模式端口回读缺失（中）

- **位置**：`electron/backend-manager.js`
- **根因**：第一轮只修了 dev 模式从 `.backend-port` 回读实际端口；打包模式因 Nuitka 产物的端口文件默认落点不可靠而未实现，端口被抢占时健康检查会盯错端口 15 秒后误判启动失败。
- **修复**：两种模式统一通过 `--port-file` 显式指定端口文件路径——打包模式写到 `userData` 目录（resources 目录在 Program Files 安装时只读），dev 模式仍为项目根目录；`_refreshPortFromFile` 去掉打包模式守卫，spawn 前统一清理残留端口文件。
- **备注**：打包链路的端到端确认需在下次执行 `npm run backend:build` + `backend:verify` 时覆盖。

### 验证汇总

| 验证项 | 结果 |
|:---|:---|
| 后端单测（43 个，新增 14 个） | ✅ 全部通过 |
| Electron 单测（6 个） | ✅ 全部通过 |
| `npm run build` | ✅ 构建成功 |
| 后端 `/health` | ✅ `{"status":"ok"}` |
| 端口占用实测 | ✅ 18000 被占用时自动启用 18001 |
| `ruff check` | ✅ 0 错误 |

---

## 2026-06-10 — 第一轮：全项目排查修复（32 项）

3 个并行审查代理逐文件通读后端 / 前端 / Electron 全部源码，发现 35 项真实缺陷，修复 32 项（余 3 项见第二轮）。提交：`ef87980`。

### 后端（16 项）

| 级别 | 问题 | 位置 |
|:---|:---|:---|
| 高 | tidevice 调用不存在的 `_kill_process`，iOS WDA 冷启动必崩 | `tidevice_adapter.py` |
| 高 | scroll 指令按旧协议打包 25 字节（scrcpy ≥1.25 为 21 字节 i16 定点），一次滚动即令控制流永久错位 | `protocol.py`, `control.py` |
| 高 | 视频流读取超时丢弃半包字节，网络抖动一次即 H.264 帧边界永久失步 | `server_manager.py` |
| 中 | control socket 阻塞模式切换竞态，异常路径不恢复非阻塞，事件循环周期性冻结 | `server_manager.py` |
| 中 | 并发 start_mirroring 竞态导致 scrcpy/WDA 双启动与资源泄漏 | `android.py`, `ios.py` |
| 中 | go-ios stop_wda 误杀多设备共享的 tunnel agent | `goios_adapter.py` |
| 中 | 长生命周期子进程 stdout/stderr=PIPE 不读取，管道写满死锁 | 两个 iOS adapter |
| 中 | tidevice 同步 usbmuxd I/O 阻塞事件循环 | `tidevice_adapter.py` |
| 中 | 设备别名热加载不生效（变更检测不含 alias） | `device_manager.py` |
| 中 | WS 订阅后首次 send 失败导致订阅队列永久泄漏 | `websocket/devices.py`, `video.py` |
| 低 | 子进程超时未 kill、create_task 无强引用、临时文件泄漏、ADB 属性失败缓存空值、截图返回类型违反契约 | 多处 |

### 前端（9 项）

| 级别 | 问题 | 位置 |
|:---|:---|:---|
| 高 | 单击发 `tap`（后端 Android 无此分支），触点永不释放且每次点击报错 | `controlCommandState.js` |
| 高 | 按键只发 down 永不发 up，约 500ms 后触发系统长按行为 | `useDeviceControl.js` 等 |
| 高 | 解码出错后重试永久黑屏失控（canvas/触控绑定残留旧 DOM 节点） | `useVideoDecoder.js`, `DeviceMirrorPanel.vue` |
| 中 | MirrorView 卸载清理是死代码，经侧边栏/浏览器返回离开不停投屏 | `MirrorView.vue` |
| 中 | useWebSocket 并发 connect 竞态、重连定时器叠加 | `useWebSocket.js` |
| 中 | 多处未等 `ready` 即发请求（Electron 生产 file:// 下必失败） | 3 个组件 |
| 低 | MJPEG 帧乱序渲染、HomeView iOS 谓词不一致、useDevices 挂起期泄漏 | 多处 |

### Electron（8 项）

| 级别 | 问题 | 位置 |
|:---|:---|:---|
| 高 | SIGKILL 兜底用 `!proc.killed` 判断（恒假），顽固进程无法强杀 | `backend-manager.js` |
| 高 | 未监听子进程 `error` 事件，spawn 失败打崩主进程 | `backend-manager.js` |
| 高 | 重启失败路径污染 `_stopping`，"最多重试 3 次"退化为 1 次 | `backend-manager.js` |
| 高 | before-quit 不等待异步 stop，优雅关闭被截断 | `main.js` |
| 中 | 崩溃重启无并发互斥，快速反复崩溃时多条重启链并行 | `backend-manager.js` |
| 中 | 健康检查 timeout+error 双重调度，轮询链指数翻倍 | `backend-manager.js` |
| 低 | 退出码 0 静默失联、dev 模式端口语义不一致 | `backend-manager.js` |

### 验证汇总

后端 29 测试 / 前端 14 测试 / Electron 6 测试全部通过；`npm run build` 成功；`/health` 返回 ok；ruff 0 错误。
