# Windows 端回归验证清单

> 用于在 Windows 真机环境逐项核对第五~七轮修复。验证前先 `git pull` 并重新打包,然后边测边在"实测结果"列填写(✅ 通过 / ❌ 失败 / ⬜ 未测)。
> 各修复项的根因与说明见 [会话纪要-2026-06-12](会话纪要-2026-06-12.md);技术细节见 [修复日志](修复日志.md)。

## 验证项

| 序号 | 验证项 | 操作步骤 | 预期结果 | 实测结果 |
|:---:|:---|:---|:---|:---:|
| 1 | **iOS 17.7.4 点击** | 投屏 17.7.4 设备,在画面上点击 | 能正常点击响应(不再只有滑动有效) | ⬜ |
| 2 | **iOS 15.1 触控位置** | 投屏 15.1 设备,点击/滑动不同位置 | 触控位置与页面响应位置一致(跟手) | ⬜ |
| 3 | **运行日志页面** | 打开侧边栏"运行日志",启动 iOS 投屏 | 日志正常显示并自动刷新;能看到 `scale≈N` 那行 | ⬜ |
| 4 | **正常关闭无残留** | 启动→连 iOS 17+ 投屏(拉起 tunnel)→关闭 MSCA→看任务管理器 | 无 `ios.exe`/`msca-backend.exe` 残留 | ⬜ |
| 5 | **异常退出后启动自愈** | 任务管理器强杀 MSCA→直接重新启动 | 旧残留进程被自动清掉,不报端口占用 | ⬜ |
| 6 | **iOS 17+ tunnel** | 投屏 iOS 17.7.4(新 go-ios v1.2.0 + 内嵌 wintun) | tunnel 正常拉起,投屏成功 | ⬜ |
| 7 | **打包不再被占用** | 验证项 4/5 通过后重新打包 | electron-builder 清理 win-unpacked 不再报文件占用 | ⬜ |

## 验证项来源对照

- 验证项 1-2:iOS 触控坐标修复(第五轮,像素→点换算)
- 验证项 3:运行日志查看功能(第五轮)
- 验证项 4:正常关闭时清理子进程(第六轮)
- 验证项 5:启动时清理残留进程(第七轮)
- 验证项 6:go-ios v1.2.0 升级 + wintun 内嵌(第七轮)
- 验证项 7:前述各项达成后打包不再被占用的最终确认

## 遇到问题怎么反馈

打开"运行日志"页面,把相关报错连同 `scale≈` 那行一起复制(或从 `%APPDATA%\MSCA\logs\backend.log` 取),写入文件即可在开发机定位。

## 验证结论

> 全部通过后在此记录日期与结论;有失败项则记录现象,反馈到开发机修复。

- 验证日期:
- 总体结论:
- 遗留问题:
